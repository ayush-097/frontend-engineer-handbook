# Component Library - Starter Code

This starter template provides the basic structure for building your component library.

## 📁 File Structure

```
starter-code/
├── index.html              # Main demo page
├── styles/
│   ├── main.css           # Main stylesheet (imports all others)
│   ├── variables.css      # CSS custom properties
│   ├── reset.css          # CSS reset
│   ├── base.css           # Base styles
│   └── components/
│       ├── buttons.css    # Button component (TODO)
│       ├── inputs.css     # Input component (TODO)
│       ├── cards.css      # Card component (TODO)
│       ├── modals.css     # Modal component (TODO)
│       └── tooltips.css   # Tooltip component (TODO)
```

## 🚀 Getting Started

1. Open `index.html` in your browser
2. Start implementing components in the `styles/components/` folder
3. Use the variables defined in `styles/variables.css`
4. Test each component as you build it

## ✅ TODO List

- [ ] Implement button styles (buttons.css)
- [ ] Implement input field styles (inputs.css)
- [ ] Implement card styles (cards.css)
- [ ] Implement modal styles (modals.css)
- [ ] Implement tooltip styles (tooltips.css)
- [ ] Add more examples to index.html
- [ ] Test responsive behavior
- [ ] Add dark mode support (bonus)

## 💡 Tips

- All CSS custom properties are in `variables.css`
- Use BEM naming convention consistently
- Test on mobile devices (or browser resize)
- Focus on accessibility (keyboard navigation, ARIA)
- Document your components as you build

Good luck! 🎨
