# Lab: Shopping Cart with Zustand

Build a persistent shopping cart with Zustand: add/remove items, update quantities, calculate totals, persist to localStorage.

**Files:** `store.ts`, `CartPage.tsx`, `ProductList.tsx`  
**Features:** Slices (cart, products), persistence middleware, selectors  
**Time:** 3–4 hours
